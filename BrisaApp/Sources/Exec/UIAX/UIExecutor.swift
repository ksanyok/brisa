import Foundation
import Cocoa

/// Исполнитель для взаимодействия с интерфейсом macOS через Accessibility API.
final class UIExecutor {
    /// Выполняет клик по переданному AXUIElement.
    func click(element: AXUIElement) {
        // TODO: реализовать взаимодействие с AXUIElement для клика
    }

    /// Вводит текст в переданный AXUIElement.
    func type(text: String, element: AXUIElement) {
        // TODO: реализовать ввод текста через Accessibility API
    }
}